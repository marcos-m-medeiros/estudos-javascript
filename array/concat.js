// Definindo duas arrays com nomes de filhas e filhos
const filhas = ["Ualeskah", "Cibelena"];
const filhos = ["Uoxiton", "Uesclei"];

// Concatenando as duas arrays em uma terceira chamada 'todos'
const todos = filhas.concat(filhos);

// Exibindo as três arrays no console
console.log(todos, filhas, filhos);

// Concatenando várias arrays e valores em uma única array e exibindo no console
console.log([].concat([1, 2], [3, 4], 5, [[6, 7]]));
